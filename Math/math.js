// Primer Ejercecio Math Methods
/*console.log(Math.PI); //Valor de Pi
console.log(Math.max(1, 2, 3, 4, 5)); //Maximo
console.log(Math.min(1, 2, 3, 4, 5)); //Minimo
console.log(Math.pow(5, 2)); //5^2=25
console.log(Math.sqrt(9)); //Raiz Cuadrada*/

//Segundo Ejercecio 
/*function getRandom() {
    //return Math.round(Math.random()) == 1 ? true : false; // ternary operator
    return Math.round(Math.random()); // Sin ternary operator
}
//damos el valor del getRandom a una nueva variable y trabajamos con la nueva var
const value = getRandom();

/*console.log(value)*/

// Tercet Ejercecio 
var piRedondeado = parseFloat(Math.PI.toFixed(3));
console.log(piRedondeado);